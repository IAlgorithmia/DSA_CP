#include <bits/stdc++.h>
using namespace std;

/*
Explanation : strings mein find function bhi hota hai, str.find("AB") == string::npos returned if not found
Backtracking problems require a certain structure, it'll just be a simple recursive solution with the recursive part should be enclosed in an isSafe if statement that proceeds only when the constraints allow so

*/

int main(){
    cout << "Hey there!" << endl;
}