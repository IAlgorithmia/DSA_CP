#include <stdio.h>
int main()
{
    int num;
    printf("Enter how many booleans are there :  \n");
    scanf("%d", &num);
    int times = 2^(num);
    for (int i = 1; i < times; i++)
    {
        for (int j = 1; j <= num; j++)
        {
            if ()
            printf("%s ")
        }
    }
    return 0;
}